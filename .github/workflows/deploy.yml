name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          GITHUB_ACTIONS: "true"

      - name: Copy Graphify Assets
        run: |
          mkdir -p dist/graphify-out
          cp graphify-out/graph.html dist/graphify-out/
          cp graphify-out/sampleData.json dist/graphify-out/

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5

      - name: Ping Search Engines with Sitemap
        run: |
          curl -s "https://www.google.com/ping?sitemap=https://yashas-30.github.io/NYX/sitemap.xml" || true
          curl -s "https://www.bing.com/ping?sitemap=https://yashas-30.github.io/NYX/sitemap.xml" || true

